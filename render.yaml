services:
  - type: web
    name: backend
    runtime: docker
    repo: https://github.com/dhruvsingh895/RideFlow
    plan: free
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: postgres
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: redis
          type: keyvalue
          property: connectionString
      - key: JWT_SECRET
        generateValue: true

  - type: web
    name: frontend
    runtime: docker
    repo: https://github.com/dhruvsingh895/RideFlow
    plan: free
    dockerfilePath: ./frontend/Dockerfile
    dockerContext: ./frontend
    healthCheckPath: /
    envVars:
      # Free-plan services can't resolve internal hostnames, so use the
      # backend's public hostname (its slug is stable: <name>-<random>).
      - key: BACKEND_URL
        value: backend-6g50.onrender.com

  - type: keyvalue
    name: redis
    plan: free
    ipAllowList: []

databases:
  - name: postgres
    plan: free
